import Vue from 'vue';

import Common from './common'

// Vue.prototype.useAll = (...plugins) => {
// Vue.prototype.useAll = (...plugins) => {
//     plugins.map((p) => {
//         if (Array.isArray(p) && p.length > 0) {
//             return p.map(_p => Vue.use(_p));
//         }
//         Vue.use(p);
//     });
// }

// console.log(Vue);
// import Common from './common';

Vue.use(Common);




export { }
